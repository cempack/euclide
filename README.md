# Euclide

**Le bureau d'enseignement** — un assistant de classe local et portable, conçu par Elliot Moreau.

```bash
npm install && npm run app   # lancer en développement
npm run app:build             # produire l'exécutable
```
